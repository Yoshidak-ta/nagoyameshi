package com.example.nagoyameshi.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ReservationRepository extends JpaRepository<Resevation,Integer> {
	public Page<Reservation> findByUserOrderByCreatedDesc(Use user, Pageable pageable);

}
